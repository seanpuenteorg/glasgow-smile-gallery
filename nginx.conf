server {
    listen $PORT;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Security headers
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/javascript text/html image/svg+xml font/woff2;
    gzip_min_length 256;

    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    # Embedded booking portal SPA — /book/ and any unknown path under it
    # (e.g. /book/cube-dental from client-side routing) fall back to /book/index.html.
    # Real assets like /book/app.js still serve normally because try_files tries $uri first.
    location /book/ {
        try_files $uri $uri/ /book/index.html;
    }

    # Cache static assets — short TTL with revalidation so client reviews see updates
    location ~* \.(css|js|png|jpg|jpeg|webp|svg|woff2|ico)$ {
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }

    # Custom 404
    error_page 404 /404.html;
}
